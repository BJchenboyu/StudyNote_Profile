1.通常效率还是比较高的

2!!!排序接口参数中的上限迭代器指向容器最后一个元素的后一个，后跟比较器。
可传入全局函数，或者在类中重载比较运算符


3 在使用类函数作为参数的时候，直接用将导致错误，正确的用法是
成员函数适配器:
当迭代器指向的是对象指针时用：mem_fun(&classname::functionname)
当迭代器指向的是对象本身时用：mem_fun_ref(&classname::functionname)